import Placeholder from "@/components/shared/Placeholder";

export default function PositionsPage() {
  return (
    <Placeholder
      title="Positions"
      subtitle="Open positions with entry context, live P&L, Greeks, signal decay, and the exact exit rules currently protecting each position."
      phase="Phase 6 (Paper Execution)"
    />
  );
}
