import Placeholder from "@/components/shared/Placeholder";

export default function RecommendationsPage() {
  return (
    <Placeholder
      title="Recommendations"
      subtitle="LLM-discovered candidates. Every card requires your explicit review — nothing enters the Watchlist automatically, and there is deliberately no Trade Now action here."
      phase="Phase 8 (LLM Recommendations)"
    />
  );
}
