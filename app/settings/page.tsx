import Placeholder from "@/components/shared/Placeholder";

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      subtitle="Account permissions, risk-profile configuration, strategy parameters, and provider credentials (server-side only)."
      phase="later phases"
    />
  );
}
