import Placeholder from "@/components/shared/Placeholder";

export default function BacktestsPage() {
  return (
    <Placeholder
      title="Backtests"
      subtitle="Configure and run historical backtests on Watchlist symbols with explicit fill models, transaction costs, and clearly separated in-sample / validation / out-of-sample results."
      phase="Phase 3 (Backtest V1)"
    />
  );
}
