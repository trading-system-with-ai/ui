import Placeholder from "@/components/shared/Placeholder";

export default function RiskPage() {
  return (
    <Placeholder
      title="Risk"
      subtitle="NAV, cash floor, Portfolio Heat, net Greeks, correlation buckets, drawdown, and kill-switch status — the page that decides whether new risk is allowed."
      phase="Phase 4 (Portfolio / Allocation / Risk)"
    />
  );
}
